# Terminal 1 - Start the agent (serves both API and UI)
cd d:\Websites\neko-claw\agent
go run main.go llm.go

# Open browser to http://localhost:8080
# Configure settings and use the AI controller!

# First setup
npm install
npm run build in ui folder

in agents folder, go run main.go